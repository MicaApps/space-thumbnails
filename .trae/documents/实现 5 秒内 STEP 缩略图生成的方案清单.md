收到，既然时间宽限到了 20 秒，我们可以采用更稳健的**“动态时间预算 + 均匀抽样”**策略。

### 实施方案

1.  **动态超时机制**：
    *   检测到模型包含大量部件（Shell > 1000）时，自动将超时阈值提升至 **18秒**（预留 2 秒用于渲染和保存）。
    *   普通小文件仍保持 **4秒** 超时，保证秒开体验。

2.  **均匀抽样 (Uniform Sampling)**：
    *   为了防止“只解析出前 10 个螺丝”的情况，我将采用**跳步处理**（例如：每隔 N 个部件处理一个）。
    *   如果在 18 秒内没跑完，就用已经解析出来的这些分布在模型各个角落的部件，拼凑出全貌。

3.  **防崩溃设计**：
    *   您之前看到的 Panic 报错（`Tolerance is larger than the radius`）会导致程序闪退。
    *   我将加上**异常捕获 (Catch Unwind)**，遇到有问题的部件直接跳过，确保程序活着把图生成出来。

我现在就开始修改代码。