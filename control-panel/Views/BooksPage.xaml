<Page
    x:Class="SpaceThumbnails.ControlPanel.Views.BooksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:SpaceThumbnails.ControlPanel"
    mc:Ignorable="d">

    <Page.Resources>
        <local:StatusToStyleConverter x:Key="StatusToStyleConverter"
            HighlightStyle="{StaticResource AccentButtonStyle}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Spacing="8">
                <TextBlock Text="Books &amp; Documents" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Enable thumbnail previews for book and document formats." 
                           Style="{StaticResource BodyTextBlockStyle}" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <CommandBar DefaultLabelPosition="Right" Background="Transparent" HorizontalAlignment="Left">
                <AppBarButton Icon="SelectAll" Label="Enable All" Click="OnEnableAllClick"/>
                <AppBarButton Icon="Clear" Label="Disable All" Click="OnDisableAllClick"/>
            </CommandBar>
        </StackPanel>

        <!-- Warning for LibreOffice -->
        <Grid Grid.Row="1" Margin="0,0,0,16" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4"
              Visibility="{x:Bind IsLibreOfficeMissing, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="LibreOffice Recommended" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock Text="To generate thumbnails for DOCX files without Microsoft Office, please install LibreOffice." 
                           Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <Grid Grid.Column="1" VerticalAlignment="Center">
                <Button Content="Download &amp; Install" Click="OnDownloadLibreOfficeClick" 
                        Style="{StaticResource AccentButtonStyle}"
                        Visibility="{x:Bind DownloadButtonVisibility, Mode=OneWay}"/>
                <StackPanel Orientation="Vertical" Spacing="4" Width="200"
                            Visibility="{x:Bind DownloadProgressVisibility, Mode=OneWay}">
                    <TextBlock Text="{x:Bind DownloadStatusText, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Left"/>
                    <ProgressBar Value="{x:Bind DownloadProgressValue, Mode=OneWay}" Maximum="100"/>
                </StackPanel>
            </Grid>
        </Grid>

        <ListView Grid.Row="2" x:Name="FormatsList" Margin="0" BorderThickness="0" CornerRadius="0">
            <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:FormatItem">
                        <Grid Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4" Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                                <Image Source="{x:Bind PreviewImage}" Width="40" Height="40" Stretch="Uniform" VerticalAlignment="Center"/>
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Extension}" Style="{StaticResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind Description}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Opacity="0.7"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="0">
                                <Button Content="默认关联" Click="OnRestoreAssociationClick" Tag="{x:Bind}" 
                                        CornerRadius="4,0,0,4" BorderThickness="1,1,0,1" Margin="0"
                                        Style="{x:Bind IsEnabled, Mode=OneWay, Converter={StaticResource StatusToStyleConverter}, ConverterParameter='Restore'}"
                                        ToolTipService.ToolTip="移除 SpaceThumbnails 关联"/>
                                <Button Content="启用缩略图" Click="OnEnableThumbnailClick" Tag="{x:Bind}" 
                                        CornerRadius="0,4,4,0" BorderThickness="0,1,1,1" Margin="0"
                                        Style="{x:Bind IsEnabled, Mode=OneWay, Converter={StaticResource StatusToStyleConverter}, ConverterParameter='Enable'}"
                                        ToolTipService.ToolTip="使用 SpaceThumbnails 生成缩略图"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
