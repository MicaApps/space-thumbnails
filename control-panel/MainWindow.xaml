<Window
    x:Class="SpaceThumbnails.ControlPanel.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:SpaceThumbnails.ControlPanel"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right" Background="Transparent">
            <AppBarButton Icon="Play" Label="注册 DLL 组件" Click="OnApplyThumbnailsClick"/>
            <AppBarButton Icon="Refresh" Label="重建图标缓存" Click="OnRebuildIconCacheClick"/>
        </CommandBar>

        <NavigationView Grid.Row="1" x:Name="NavView" 
                        SelectionChanged="NavView_SelectionChanged"
                        Loaded="NavView_Loaded"
                        PaneDisplayMode="Left">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="View" Content="3D Models" Tag="ThreeDPage" />
                <NavigationViewItem Icon="Pictures" Content="Images" Tag="ImagesPage" />
                <NavigationViewItem Icon="Document" Content="Text &amp; Code" Tag="TextPage" />
                <NavigationViewItem Icon="Library" Content="Books" Tag="BooksPage" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <!-- <NavigationViewItem Icon="Setting" Content="Settings" Tag="GeneralPage" /> -->
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
