<Window
    x:Class="SpaceThumbnails.ControlPanel.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:SpaceThumbnails.ControlPanel"
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <local:StatusToStyleConverter x:Key="StatusToStyleConverter"
                HighlightStyle="{StaticResource AccentButtonStyle}" />
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
            <AppBarButton x:Uid="Btn_RebuildCache" Icon="Refresh" Label="重建图标缓存" Click="RebuildIconCache_Click"/>
            <AppBarButton x:Uid="Btn_RegisterDLL" Icon="Play" Label="注册 DLL 组件" Click="OnApplyThumbnailsClick"/>
        </CommandBar>

        <NavigationView Grid.Row="1" x:Name="NavView" PaneDisplayMode="Left" SelectionChanged="NavView_SelectionChanged" Loaded="NavView_Loaded" IsBackButtonVisible="Collapsed" IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="Nav_3D" Content="3D Models" Tag="3d" Icon="View" />
                <NavigationViewItem x:Uid="Nav_Images" Content="Images" Tag="images" Icon="Pictures" />
                <NavigationViewItem x:Uid="Nav_Books" Content="Books" Tag="books" Icon="Library" />
                <NavigationViewItem x:Uid="Nav_Documents" Content="Documents" Tag="document" Icon="Document" />
            </NavigationView.MenuItems>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,12,0,12" Spacing="20">
                    <TextBlock x:Name="StatusText" Text="" Style="{StaticResource SubtitleTextBlockStyle}"/>
                </StackPanel>

                <ListView Grid.Row="1" x:Name="FormatsList" Margin="0" BorderThickness="0" CornerRadius="0">
                    <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                                        <Grid Width="32" Height="32">
                                            <FontIcon Glyph="&#xE8B9;" FontSize="20" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" 
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Visibility="{Binding ShowIcon}"/>
                                            <Image Source="{Binding PreviewImage}" Stretch="Uniform" 
                                                   Visibility="{Binding ShowPreview}"/>
                                        </Grid>
                                        <TextBlock Text="{Binding Extension}" Style="{StaticResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Guid}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Opacity="0.7"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="0">
                                        <Button x:Uid="Btn_Restore" Click="OnRestoreAssociationClick" Tag="{Binding}" 
                                                CornerRadius="4,0,0,4" BorderThickness="1,1,0,1" Margin="0"
                                                Style="{Binding IsEnabled, Converter={StaticResource StatusToStyleConverter}, ConverterParameter='Restore'}"/>
                                        <Button x:Uid="Btn_Enable" Click="OnEnableThumbnailClick" Tag="{Binding}" 
                                                CornerRadius="0,4,4,0" BorderThickness="0,1,1,1" Margin="0"
                                                Style="{Binding IsEnabled, Converter={StaticResource StatusToStyleConverter}, ConverterParameter='Enable'}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
